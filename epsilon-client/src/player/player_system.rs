use crate::player::player_scene::PlayerExt;
use epsilon_core::IPlayer;

impl IPlayer for PlayerExt {
    fn respawn(&mut self) {

    }

    fn game_over(&mut self) {

    }

    fn damage(&mut self, damage: i16) {

    }
}